import React from 'react';

const Header = () => {
    return (
        <header>
            <h1 style={{color:'white', fontFamily:'monospace'}}>To Do List</h1>
        </header>
    );
};

export default Header;